# Backend - QuickNotes API

Backend сервис для QuickNotes, построенный на Node.js, TypeScript, Express и PostgreSQL.

## Технологии

- **Node.js** + **TypeScript**
- **Express** - веб-фреймворк
- **PostgreSQL** (Neon) - база данных
- **Prisma** - ORM
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей
- **Zod** - валидация данных и переменных окружения

## Структура проекта

```
backend/
├── src/
│   ├── controllers/     # Контроллеры для обработки запросов
│   ├── middlewares/      # Express middleware (auth, errorHandler)
│   ├── routes/          # Маршруты API
│   ├── config/          # Конфигурация (env.ts, index.ts)
│   ├── utils/           # Утилиты (Prisma, response)
│   ├── app.ts           # Инициализация Express приложения
│   └── server.ts        # Точка входа, запуск сервера
├── prisma/
│   └── schema.prisma    # Схема базы данных
├── .env.example         # Пример файла с переменными окружения
├── package.json
└── tsconfig.json
```

## Локальный запуск

### Предварительные требования

- Node.js 18+ и npm
- PostgreSQL база данных (рекомендуется Neon)
- Переменные окружения настроены в `.env`

### Шаг 1: Установка зависимостей

```powershell
npm install
```

### Шаг 2: Настройка переменных окружения

```powershell
Copy-Item .env.example .env
```

Отредактируйте `.env` и укажите:
- `DATABASE_URL` - строка подключения к PostgreSQL (Neon)
- `JWT_SECRET` - секретный ключ для JWT токенов (минимум 32 символа)

### Шаг 3: Настройка базы данных

```powershell
# Генерация Prisma Client
npm run prisma:generate

# Применение миграций (создание таблиц)
npm run prisma:migrate
# Введите имя миграции: init
```

### Шаг 4: Запуск сервера

```powershell
# Режим разработки (с hot-reload)
npm run dev

# Или production сборка
npm run build
npm start
```

Сервер будет доступен на `http://localhost:4000` (или порт из `PORT` в `.env`)

## Деплой на Node hosting

### Railway

1. **Подключите репозиторий к Railway**
   - Создайте проект в Railway
   - Подключите GitHub репозиторий
   - Выберите директорию `packages/backend`

2. **Настройте переменные окружения в Railway:**
   ```
   DATABASE_URL=<ваша строка подключения к PostgreSQL>
   JWT_SECRET=<секретный ключ минимум 32 символа>
   JWT_EXPIRES_IN=7d
   NODE_ENV=production
   PORT=4000
   CORS_ORIGIN=<ваш домен или *>
   ```

3. **Настройте Build Command:**
   ```
   npm install && npm run prisma:generate && npm run build
   ```

4. **Настройте Start Command:**
   ```
   npm run prisma:migrate deploy && npm start
   ```

5. **Применение миграций:**
   - Railway автоматически выполнит `prisma migrate deploy` при деплое
   - Или выполните вручную через Railway CLI:
     ```powershell
     railway run npm run prisma:migrate deploy
     ```

### Vercel (Serverless)

1. **Создайте `vercel.json` в корне backend:**
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "dist/server.js",
         "use": "@vercel/node"
       }
     ],
     "routes": [
       {
         "src": "/(.*)",
         "dest": "dist/server.js"
       }
     ]
   }
   ```

2. **Настройте переменные окружения в Vercel:**
   ```
   DATABASE_URL=<ваша строка подключения>
   JWT_SECRET=<секретный ключ>
   JWT_EXPIRES_IN=7d
   NODE_ENV=production
   CORS_ORIGIN=<ваш домен>
   ```

3. **Обновите `package.json` scripts для Vercel:**
   ```json
   {
     "scripts": {
       "vercel-build": "npm run prisma:generate && npm run build"
     }
   }
   ```

4. **Применение миграций:**
   ```powershell
   vercel env pull .env.local
   npx prisma migrate deploy
   ```

### Render

1. **Создайте новый Web Service**
   - Подключите репозиторий
   - Root Directory: `packages/backend`

2. **Настройте Build Command:**
   ```
   npm install && npm run prisma:generate && npm run build
   ```

3. **Настройте Start Command:**
   ```
   npm run prisma:migrate deploy && npm start
   ```

4. **Настройте переменные окружения:**
   ```
   DATABASE_URL=<ваша строка подключения>
   JWT_SECRET=<секретный ключ>
   JWT_EXPIRES_IN=7d
   NODE_ENV=production
   PORT=4000
   ```

### Heroku

1. **Создайте `Procfile` в корне backend:**
   ```
   release: npm run prisma:migrate deploy
   web: npm start
   ```

2. **Настройте переменные окружения:**
   ```powershell
   heroku config:set DATABASE_URL=<ваша строка подключения>
   heroku config:set JWT_SECRET=<секретный ключ>
   heroku config:set JWT_EXPIRES_IN=7d
   heroku config:set NODE_ENV=production
   ```

3. **Деплой:**
   ```powershell
   git subtree push --prefix packages/backend heroku main
   ```

## Переменные окружения

Все переменные окружения валидируются через Zod при запуске приложения.

### Обязательные

- `DATABASE_URL` - строка подключения к PostgreSQL (валидный URL)
- `JWT_SECRET` - секретный ключ для JWT токенов (минимум 32 символа)

### Опциональные

- `PORT` - порт сервера (по умолчанию 4000)
- `NODE_ENV` - окружение: `development`, `production`, `test` (по умолчанию `development`)
- `JWT_EXPIRES_IN` - срок жизни токена (по умолчанию `7d`)
- `CORS_ORIGIN` - разрешенный origin для CORS (по умолчанию `*`)

Пример `.env`:
```env
DATABASE_URL="postgresql://user:password@host.neon.tech/database?sslmode=require"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"
PORT=4000
NODE_ENV=development
CORS_ORIGIN="*"
```

## API Endpoints

### Аутентификация

- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение текущего пользователя (требует JWT)

### Заметки (требуют JWT)

- `GET /api/notes` - Получить список заметок (параметр `includeArchived=true` для всех)
- `GET /api/notes/:id` - Получить заметку по ID
- `POST /api/notes` - Создать новую заметку
- `PUT /api/notes/:id` - Обновить заметку
- `DELETE /api/notes/:id` - Мягкое удаление заметки

Подробная документация API: [API.md](./API.md)

## База данных

Схема базы данных определена в `prisma/schema.prisma`:

- **User** - пользователи (email, passwordHash)
- **Note** - заметки (title, content, isArchived, deletedAt, timestamps)

### Команды Prisma

```powershell
# Генерация Prisma Client
npm run prisma:generate

# Создание и применение миграций (dev)
npm run prisma:migrate

# Применение миграций (production)
npx prisma migrate deploy

# Просмотр базы данных в Prisma Studio
npm run prisma:studio

# Сброс базы данных (осторожно!)
npm run prisma:reset
```

## Разработка

```powershell
# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск собранного проекта
npm start

# Проверка кода
npm run lint

# Форматирование кода
npm run format
```

## Логирование

При запуске сервера выводится:
- ✅ Подключение к базе данных
- 🚀 Информация о запуске сервера
- 📝 Окружение и порт
- 🌐 URL для доступа к API

При graceful shutdown:
- ✅ Отключение от базы данных
- ✅ Закрытие HTTP сервера

## Troubleshooting

### Ошибка валидации переменных окружения

Если при запуске появляется ошибка валидации, проверьте:
- Все обязательные переменные установлены
- `JWT_SECRET` содержит минимум 32 символа
- `DATABASE_URL` является валидным URL

### Ошибка подключения к базе данных

- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что база данных доступна
- Проверьте сетевое подключение

### Миграции не применяются

- Убедитесь, что Prisma Client сгенерирован: `npm run prisma:generate`
- Проверьте права доступа к базе данных
- Для production используйте: `npx prisma migrate deploy`
