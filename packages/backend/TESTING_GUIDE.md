# Руководство по тестированию QuickNotes Backend

## 📚 Содержание

1. [Автоматические тесты](#автоматические-тесты)
2. [Ручное тестирование](#ручное-тестирование)
3. [Запуск тестов](#запуск-тестов)
4. [Примеры использования](#примеры-использования)

## 🧪 Автоматические тесты

### Структура тестов

```
src/__tests__/
├── controllers/
│   └── notesController.test.ts      # 20+ тестов для контроллера заметок
├── middlewares/
│   ├── auth.test.ts                 # 12+ юнит-тестов для auth middleware
│   └── auth.integration.test.ts    # 10+ интеграционных тестов
└── helpers/
    ├── prisma.mock.ts               # Мок Prisma Client
    └── test-helpers.ts              # Вспомогательные функции
```

### Покрытие функционала

#### ✅ Контроллер заметок (`notesController.test.ts`)

**Получение заметок:**
- ✅ Получение списка заметок текущего пользователя
- ✅ Фильтрация архивных заметок
- ✅ Включение архивных заметок при `includeArchived=true`
- ✅ Фильтрация удаленных заметок (`deletedAt !== null`)
- ✅ Изоляция данных между пользователями
- ✅ Получение заметки по ID
- ✅ Обработка несуществующей заметки (404)

**Создание заметок:**
- ✅ Создание новой заметки с заголовком и содержимым
- ✅ Создание заметки только с заголовком (пустое содержимое по умолчанию)
- ✅ Валидация обязательного поля `title`
- ✅ Привязка заметки к текущему пользователю (`userId`)

**Обновление заметок:**
- ✅ Обновление заголовка и содержимого
- ✅ Частичное обновление (только заголовок или только содержимое)
- ✅ Архивирование заметки (`isArchived: true`)
- ✅ Обработка несуществующей заметки (404)
- ✅ Защита от обновления чужих заметок

**Удаление заметок:**
- ✅ Мягкое удаление (установка `deletedAt`)
- ✅ Обработка несуществующей заметки (404)
- ✅ Защита от удаления чужих заметок

**Безопасность:**
- ✅ Проверка авторизации (401 при отсутствии пользователя)
- ✅ Изоляция данных между пользователями
- ✅ Обработка ошибок базы данных

#### ✅ Auth Middleware (`auth.test.ts`)

**Валидация токенов:**
- ✅ Пропуск запроса с валидным токеном
- ✅ Обработка отсутствующего токена (401)
- ✅ Обработка неверного формата токена (401)
- ✅ Обработка истекшего токена (401)
- ✅ Обработка недействительного токена (401)
- ✅ Обработка токена с неправильным секретом (401)
- ✅ Обработка не-JWT токена (401)
- ✅ Обработка пустого токена после `Bearer` (401)

**Конфигурация:**
- ✅ Обработка отсутствия JWT secret (500)

**Установка пользователя:**
- ✅ Извлечение токена из заголовка `Authorization`
- ✅ Установка `req.user` с данными из токена
- ✅ Корректная передача управления следующему middleware

#### ✅ Интеграционные тесты (`auth.integration.test.ts`)

**Интеграция с JWT:**
- ✅ Работа с реальным JWT токеном
- ✅ Обработка истекшего токена
- ✅ Работа с разными алгоритмами (HS256)

**Интеграция с Express:**
- ✅ Работа с различными форматами заголовков
- ✅ Обработка отсутствия заголовка
- ✅ Установка `req.user` для последующих middleware

**Граничные случаи:**
- ✅ Обработка очень длинного токена
- ✅ Обработка токена с специальными символами
- ✅ Обработка токена с unicode символами

## 📋 Ручное тестирование

Подробный чек-лист находится в файле [`TESTING_CHECKLIST.md`](./TESTING_CHECKLIST.md).

### Основные разделы:

1. **Регистрация нового пользователя**
   - Успешная регистрация
   - Регистрация с существующим email
   - Валидация полей

2. **Логин**
   - Успешный вход
   - Неверный пароль
   - Несуществующий пользователь
   - Валидация полей

3. **Создание заметки**
   - Создание новой заметки
   - Создание заметки только с заголовком
   - Валидация заголовка
   - Проверка отображения в списке

4. **Редактирование заметки**
   - Редактирование существующей заметки
   - Частичное обновление
   - Архивирование заметки
   - Фильтр архивных заметок
   - Разархивирование

5. **Удаление/Архивация**
   - Мягкое удаление заметки
   - Отмена удаления
   - Проверка недоступности удаленной заметки
   - Попытка удаления несуществующей заметки
   - Попытка удаления заметки другого пользователя

6. **Синхронизация между устройствами**
   - Вход с другого устройства
   - Создание заметки на втором устройстве
   - Редактирование на разных устройствах
   - Одновременное редактирование
   - Проверка JWT токена
   - Проверка изоляции данных между пользователями

7. **Дополнительные тесты**
   - Pull-to-refresh
   - Выход из аккаунта
   - Повторный вход после выхода
   - Обработка ошибок сети
   - Истечение токена
   - Валидация данных на сервере
   - Проверка производительности
   - Обработка специальных символов

## 🚀 Запуск тестов

### Все тесты
```powershell
cd packages/backend
npm test
```

### Тесты в watch режиме (автоматический перезапуск)
```powershell
npm run test:watch
```

### Тесты с покрытием кода
```powershell
npm run test:coverage
```

Отчет о покрытии будет доступен в:
- HTML: `coverage/index.html`
- LCOV: `coverage/lcov.info`
- Текст: в консоли

### Конкретный файл тестов
```powershell
# Тесты контроллера заметок
npm test -- notesController.test.ts

# Тесты auth middleware
npm test -- auth.test.ts

# Интеграционные тесты auth
npm test -- auth.integration.test.ts
```

### Конкретный тест
```powershell
npm test -- -t "должен создавать новую заметку"
```

## 📊 Статистика тестов

**Текущее состояние:**
- ✅ **44 теста** проходят успешно
- ✅ **3 test suites** (все проходят)
- ✅ Покрытие: проверьте через `npm run test:coverage`

**Распределение:**
- `notesController.test.ts`: ~20 тестов
- `auth.test.ts`: ~12 тестов
- `auth.integration.test.ts`: ~10 тестов

## 💡 Примеры использования

### Пример 1: Добавление нового теста

```typescript
// В файле notesController.test.ts
describe('createNote', () => {
  it('должен создавать заметку с длинным содержимым', async () => {
    const user = createMockUser();
    const longContent = 'x'.repeat(10000);
    const noteData = { title: 'Long Note', content: longContent };
    const createdNote = createMockNote({ ...noteData, userId: user.id });

    mockRequest = createMockAuthRequest(user);
    mockRequest.body = noteData;
    mockPrisma.note.create.mockResolvedValue(createdNote);

    await createNote(mockRequest as Request, mockResponse as Response, mockNext);

    expect(mockPrisma.note.create).toHaveBeenCalled();
    expect(mockResponse.status).toHaveBeenCalledWith(201);
  });
});
```

### Пример 2: Тестирование ошибок

```typescript
it('должен обрабатывать ошибку базы данных', async () => {
  const user = createMockUser();
  mockRequest = createMockAuthRequest(user);
  mockRequest.body = { title: 'Test Note' };

  const dbError = new Error('Database connection failed');
  mockPrisma.note.create.mockRejectedValue(dbError);

  await createNote(mockRequest as Request, mockResponse as Response, mockNext);

  expect(mockNext).toHaveBeenCalledWith(dbError);
});
```

### Пример 3: Использование вспомогательных функций

```typescript
import { createMockUser, createMockNote, createMockAuthRequest } from '../helpers/test-helpers';

const user = createMockUser({ id: 'custom-id', email: 'custom@test.com' });
const note = createMockNote({ id: 'note-1', userId: user.id, title: 'Custom Title' });
const request = createMockAuthRequest(user);
```

## 🔧 Отладка тестов

### Проблема: Тесты не запускаются
```powershell
# Очистить кеш Jest
npm test -- --clearCache
```

### Проблема: Моки не работают
- Проверьте порядок импортов (моки должны быть до импорта тестируемого модуля)
- Убедитесь, что используете `jest.mock()` перед импортом

### Проблема: Тесты падают с ошибками Prisma
- Убедитесь, что все моки Prisma настроены правильно
- Проверьте, что используете `createMockPrisma()` из helpers

### Проблема: Тесты медленные
```powershell
# Запустить только измененные тесты
npm test -- --onlyChanged

# Запустить тесты параллельно (по умолчанию)
npm test -- --maxWorkers=4
```

## 📝 Чек-лист перед коммитом

- [ ] Все тесты проходят (`npm test`)
- [ ] Покрытие кода > 80% (`npm run test:coverage`)
- [ ] Нет линтер ошибок (`npm run lint`)
- [ ] Код отформатирован (`npm run format`)
- [ ] Новые тесты добавлены для нового функционала
- [ ] Документация обновлена (если нужно)

## 🔗 Связанные документы

- [`TESTING_CHECKLIST.md`](./TESTING_CHECKLIST.md) - Чек-лист ручного тестирования
- [`TEST_SUMMARY.md`](./TEST_SUMMARY.md) - Сводка автоматических тестов
- [`API.md`](./API.md) - Документация API
- [`AUTH.md`](./AUTH.md) - Документация по аутентификации

