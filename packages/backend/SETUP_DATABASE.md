# Настройка базы данных для QuickNotes Backend

## Вариант 1: Neon (Облачная PostgreSQL) - Рекомендуется ⭐

### Шаги:

1. **Создайте аккаунт на Neon:**
   - Перейдите на https://neon.tech
   - Зарегистрируйтесь (можно через GitHub)

2. **Создайте проект:**
   - Нажмите "Create Project"
   - Выберите регион (ближайший к вам)
   - Нажмите "Create Project"

3. **Получите строку подключения:**
   - В дашборде проекта найдите "Connection string"
   - Скопируйте строку (она выглядит так: `postgresql://user:password@host.neon.tech/database?sslmode=require`)

4. **Обновите `.env` файл:**
   ```env
   DATABASE_URL="postgresql://user:password@host.neon.tech/database?sslmode=require"
   JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
   PORT=4000
   NODE_ENV=development
   ```

5. **Примените миграции:**
   ```powershell
   cd packages\backend
   npm run prisma:generate
   npm run prisma:migrate
   ```

6. **Запустите сервер:**
   ```powershell
   npm run dev
   ```

## Вариант 2: Локальный PostgreSQL

### Установка PostgreSQL:

1. **Скачайте и установите PostgreSQL:**
   - Скачайте с https://www.postgresql.org/download/windows/
   - Установите (запомните пароль для пользователя `postgres`)

2. **Создайте базу данных:**
   ```powershell
   # Подключитесь к PostgreSQL
   psql -U postgres
   
   # Создайте базу данных
   CREATE DATABASE quicknotes;
   
   # Выйдите
   \q
   ```

3. **Обновите `.env` файл:**
   ```env
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/quicknotes"
   JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
   PORT=4000
   NODE_ENV=development
   ```

4. **Примените миграции:**
   ```powershell
   cd packages\backend
   npm run prisma:generate
   npm run prisma:migrate
   ```

5. **Запустите сервер:**
   ```powershell
   npm run dev
   ```

## Проверка подключения

После настройки базы данных проверьте подключение:

```powershell
cd packages\backend
npm run dev
```

Если все настроено правильно, вы увидите:
```
✅ Подключение к базе данных установлено
🚀 Сервер запущен на порту 4000
```

## Troubleshooting

### Ошибка "Can't reach database server"
- Проверьте, что база данных запущена (для локального PostgreSQL)
- Проверьте строку подключения в `.env`
- Убедитесь, что пароль правильный
- Для Neon: проверьте, что проект активен

### Ошибка "Authentication failed"
- Проверьте пароль в строке подключения
- Для локального PostgreSQL: убедитесь, что пользователь `postgres` существует

### Ошибка "Database does not exist"
- Создайте базу данных вручную (для локального PostgreSQL)
- Для Neon: база создается автоматически

