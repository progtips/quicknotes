# Mobile/Web App - QuickNotes

Кроссплатформенное приложение QuickNotes для iOS, Android и Web, построенное на React Native, Expo и TypeScript.

## 🚀 Быстрый старт

```powershell
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start
```

## 📋 Содержание

- [Технологии](#технологии)
- [Установка](#установка)
- [Переменные окружения](#переменные-окружения)
- [Запуск в режиме разработки](#запуск-в-режиме-разработки)
- [Сборка приложений](#сборка-приложений)
  - [Web версия (Expo Web)](#web-версия-expo-web)
  - [Android (APK/AAB)](#android-apk-aab)
  - [iOS (IPA)](#ios-ipa)
- [Деплой](#деплой)
- [Разработка](#разработка)
- [Troubleshooting](#troubleshooting)

## 🛠 Технологии

- **React Native** - фреймворк для мобильных приложений
- **Expo** - инструменты и сервисы для разработки
- **React Native Web** - поддержка веб-платформы
- **TypeScript** - типизация
- **@tanstack/react-query** - управление состоянием сервера и кеширование
- **Axios** - HTTP клиент для API запросов
- **AsyncStorage** - локальное хранилище

## 📦 Установка

```powershell
# Перейти в директорию проекта
cd packages/mobile

# Установить зависимости
npm install
```

## 🔧 Переменные окружения

Создайте файл `.env` в корне `packages/mobile`:

```env
# URL backend API
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000/api

# Окружение (development/production)
NODE_ENV=development
```

Для production укажите URL вашего backend сервера:

```env
EXPO_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api
NODE_ENV=production
```

## 🏃 Запуск в режиме разработки

### Интерактивное меню (рекомендуется)

```powershell
npm start
```

Откроется меню Expo Dev Tools с опциями:
- `w` - запуск на Web
- `a` - запуск на Android (требует эмулятор или подключенное устройство)
- `i` - запуск на iOS (требует macOS и симулятор)
- `r` - перезагрузка приложения

### Запуск на конкретной платформе

```powershell
# Web браузер
npm run web

# Android эмулятор/устройство
npm run android

# iOS симулятор (только macOS)
npm run ios

# Dev Client (для кастомных нативных модулей)
npm run dev
```

### Запуск с туннелем (для доступа через expo.dev)

```powershell
npm run start:tunnel
```

Требуется авторизация в Expo:
```powershell
npx expo login
```

## 📱 Сборка приложений

### Web версия (Expo Web)

#### Локальная сборка для разработки

```powershell
# Запуск dev сервера для Web
npm run web

# Приложение откроется в браузере на http://localhost:8081
```

#### Production сборка для деплоя

```powershell
# Сборка статических файлов
npm run build:web

# Результат будет в директории web-build/
```

После сборки в директории `web-build/` будут готовые статические файлы для деплоя на любой хостинг.

**Структура результата:**
```
web-build/
├── index.html          # Главный HTML файл
├── assets/             # JS, CSS и другие ресурсы
└── ...
```

**Деплой собранных файлов:**

1. **Vercel:**
   ```powershell
   # Установить Vercel CLI (если еще не установлен)
   npm i -g vercel

   # Деплой из директории web-build
   cd web-build
   vercel --prod
   ```

2. **Netlify:**
   ```powershell
   # Установить Netlify CLI (если еще не установлен)
   npm i -g netlify-cli

   # Деплой из директории web-build
   cd web-build
   netlify deploy --prod --dir=.
   ```

3. **Любой статический хостинг:**
   - Загрузите содержимое `web-build/` на ваш хостинг
   - Убедитесь, что `index.html` настроен как точка входа

### Android (APK/AAB)

#### Требования

1. **Expo Account** - зарегистрируйтесь на [expo.dev](https://expo.dev)
2. **EAS CLI** - установите глобально:
   ```powershell
   npm install -g eas-cli
   ```
3. **Авторизация в Expo:**
   ```powershell
   eas login
   ```

#### Настройка EAS Build

Создайте файл `eas.json` в корне `packages/mobile`:

```json
{
  "cli": {
    "version": ">= 5.0.0"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
```

#### Сборка APK (для тестирования)

```powershell
# Сборка APK для тестирования
eas build --platform android --profile preview

# Или для production
eas build --platform android --profile production
```

**Процесс сборки:**
1. EAS загрузит ваш код на облачные серверы
2. Соберет Android приложение
3. Предоставит ссылку для скачивания APK

**Скачивание APK:**
- После завершения сборки вы получите ссылку
- Скачайте APK файл
- Установите на Android устройство (разрешите установку из неизвестных источников)

#### Сборка AAB (для Google Play Store)

```powershell
# Сборка AAB для публикации в Google Play
eas build --platform android --profile production
```

В `eas.json` измените `buildType` на `"aab"`:

```json
{
  "build": {
    "production": {
      "android": {
        "buildType": "aab"
      }
    }
  }
}
```

**Публикация в Google Play:**
```powershell
# После успешной сборки
eas submit --platform android
```

### iOS (IPA)

#### Требования

1. **Apple Developer Account** ($99/год)
2. **macOS** (для локальной сборки) или используйте EAS Build в облаке
3. **EAS CLI** установлен и авторизован

#### Настройка EAS Build для iOS

Убедитесь, что в `eas.json` есть конфигурация для iOS:

```json
{
  "build": {
    "production": {
      "ios": {
        "bundleIdentifier": "com.quicknotes.app"
      }
    }
  }
}
```

#### Сборка IPA через EAS Build

```powershell
# Сборка для тестирования (TestFlight)
eas build --platform ios --profile preview

# Сборка для App Store
eas build --platform ios --profile production
```

**Процесс сборки:**
1. EAS запросит учетные данные Apple Developer
2. Загрузит код на облачные серверы
3. Соберет iOS приложение
4. Предоставит ссылку для скачивания IPA или автоматически загрузит в TestFlight

#### Публикация в App Store

```powershell
# После успешной сборки
eas submit --platform ios
```

EAS автоматически:
- Загрузит приложение в App Store Connect
- Создаст новую версию (если нужно)
- Отправит на ревью (если настроено)

## 🚀 Деплой

Подробные инструкции по деплою всех компонентов находятся в файле [`DEPLOYMENT.md`](./DEPLOYMENT.md).

### Краткая сводка

#### Backend (Node.js хостинг)

**Рекомендуемые платформы:**
- Railway
- Render
- Heroku
- DigitalOcean App Platform
- AWS Elastic Beanstalk

**Быстрый деплой на Railway:**

```powershell
# Установить Railway CLI
npm i -g @railway/cli

# Войти в Railway
railway login

# В директории backend
cd ../../packages/backend
railway init
railway up
```

#### Web версия

**Vercel (рекомендуется):**
```powershell
cd packages/mobile
npm run build:web
cd web-build
vercel --prod
```

**Netlify:**
```powershell
cd packages/mobile
npm run build:web
cd web-build
netlify deploy --prod --dir=.
```

#### Mobile версии

**Android (Google Play Store):**
1. Соберите AAB через EAS Build
2. Загрузите в Google Play Console
3. Заполните информацию о приложении
4. Отправьте на ревью

**iOS (App Store):**
1. Соберите IPA через EAS Build
2. Используйте `eas submit` для автоматической загрузки
3. Заполните информацию в App Store Connect
4. Отправьте на ревью

## 💻 Разработка

### Структура проекта

```
mobile/
├── src/
│   ├── components/        # React компоненты
│   ├── screens/           # Экраны приложения
│   │   ├── auth/         # Экраны авторизации
│   │   └── app/          # Основные экраны
│   ├── navigation/       # Навигация
│   ├── services/         # API сервисы
│   ├── contexts/         # React Context
│   ├── storage/          # Локальное хранилище
│   └── config/           # Конфигурация
├── assets/               # Изображения и иконки
├── app.json              # Конфигурация Expo
├── app.config.ts         # Динамическая конфигурация
├── package.json
└── tsconfig.json
```

### Экраны приложения

#### Авторизация
- `src/screens/auth/LoginScreen.tsx` - Вход в систему
- `src/screens/auth/RegisterScreen.tsx` - Регистрация

#### Основные экраны
- `src/screens/app/NotesListScreen.tsx` - Список заметок
- `src/screens/app/NoteEditScreen.tsx` - Редактирование заметки
- `src/screens/app/SettingsScreen.tsx` - Настройки

### API Интеграция

Все API запросы идут через `src/services/api.ts` с автоматическим добавлением JWT токена.

**Сервисы:**
- `src/services/auth.ts` - регистрация, вход, получение текущего пользователя
- `src/services/notes.ts` - CRUD операции для заметок

### React Query

Используется `@tanstack/react-query` для:
- Кеширования данных
- Автоматической синхронизации
- Управления состоянием загрузки
- Инвалидации кеша при мутациях

## 🔍 Troubleshooting

### Ошибка подключения к API

**Проблема:** Приложение не может подключиться к backend

**Решение:**
1. Проверьте, что backend сервер запущен
2. Убедитесь, что `EXPO_PUBLIC_API_BASE_URL` указан правильно
3. Для локального тестирования используйте:
   - `http://localhost:4000/api` (для эмулятора/симулятора)
   - IP адрес вашего компьютера (для реальных устройств)
   - Например: `http://192.168.1.100:4000/api`

### Проблемы с сборкой Web

**Проблема:** Ошибки при сборке web версии

**Решение:**
```powershell
# Очистить кеш
npx expo start --web --clear

# Переустановить зависимости
rm -rf node_modules
npm install
```

### Проблемы с EAS Build

**Проблема:** Ошибки при сборке через EAS

**Решение:**
1. Проверьте авторизацию: `eas whoami`
2. Убедитесь, что `eas.json` настроен правильно
3. Проверьте логи сборки: `eas build:list`
4. Для iOS убедитесь, что Apple Developer аккаунт настроен

### Проблемы с навигацией

**Проблема:** Навигация не работает корректно

**Решение:**
1. Убедитесь, что все файлы в `src/screens/` имеют правильные имена
2. Проверьте структуру навигации в `src/navigation/`
3. Проверьте, что `RootNavigator` правильно определяет авторизованного пользователя

### Ошибки TypeScript

**Проблема:** Ошибки типов при компиляции

**Решение:**
```powershell
# Обновить зависимости Expo
npx expo install --fix

# Проверить типы
npx tsc --noEmit
```

## 📚 Дополнительные ресурсы

- [Expo Documentation](https://docs.expo.dev)
- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [React Native Documentation](https://reactnative.dev)
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Подробные инструкции по деплою

## 📄 Лицензия

MIT
