# Инструкция по исправлению несовместимых версий React

## ✅ Выполненные изменения

### 1. Обновлены версии в `packages/mobile/package.json`:
- `react`: `^18.2.0` → `18.3.1` (точная версия для Expo SDK 54)
- `react-dom`: `^18.2.0` → `18.3.1` (точная версия для Expo SDK 54)
- `react-native-web`: `^0.21.0` → `~0.20.0` (совместимая версия)
- `@types/react`: `~18.2.0` → `~18.3.0` (обновлены типы)

### 2. Добавлены `overrides` в `packages/mobile/package.json`:
```json
"overrides": {
  "react": "18.3.1",
  "react-dom": "18.3.1"
}
```

### 3. Добавлены `overrides` в корневой `package.json`:
```json
"overrides": {
  "react": "18.3.1",
  "react-dom": "18.3.1",
  "react-native-web": "~0.20.0"
}
```

## 🔧 Следующие шаги (выполните вручную)

### Шаг 1: Удалите node_modules и lock-файлы

**PowerShell:**
```powershell
# Удалить все node_modules
Remove-Item -Recurse -Force node_modules
Remove-Item -Recurse -Force packages\backend\node_modules
Remove-Item -Recurse -Force packages\mobile\node_modules

# Удалить lock-файлы
Remove-Item -Force package-lock.json
Remove-Item -Force packages\backend\package-lock.json -ErrorAction SilentlyContinue
Remove-Item -Force packages\mobile\package-lock.json -ErrorAction SilentlyContinue
```

**Или вручную:**
- Удалите папки `node_modules` в корне и в `packages/backend` и `packages/mobile`
- Удалите файлы `package-lock.json` везде, где они есть

### Шаг 2: Переустановите зависимости

**PowerShell:**
```powershell
# В корне проекта
npm install

# Или если используете pnpm:
# pnpm install
```

### Шаг 3: Очистите кэш Metro и запустите приложение

**PowerShell:**
```powershell
cd packages\mobile
npx expo start --clear --web
```

## 📋 Проверка версий после установки

После установки проверьте, что установлены правильные версии:

```powershell
cd packages\mobile
npm list react react-dom react-native-web --depth=0
```

Должны быть:
- `react@18.3.1`
- `react-dom@18.3.1`
- `react-native-web@0.20.x`

## ⚠️ Важные замечания

1. **Монорепозиторий**: Overrides в корневом `package.json` гарантируют, что все пакеты используют одинаковые версии React и React DOM, даже если они установлены в разных местах.

2. **Expo SDK 54**: Версии React 18.3.1 официально поддерживаются Expo SDK 54.

3. **react-native-web**: Версия 0.20.x совместима с React 18.3.1 и Expo SDK 54.

4. **Кэш**: Обязательно используйте флаг `--clear` при запуске Expo, чтобы очистить кэш Metro bundler.

## 🐛 Если проблема сохраняется

1. Убедитесь, что удалили все `node_modules` и lock-файлы
2. Проверьте, что в корневом `package.json` нет прямых зависимостей от `react` или `react-dom`
3. Попробуйте очистить кэш npm:
   ```powershell
   npm cache clean --force
   ```
4. Проверьте консоль браузера на наличие других ошибок

## ✅ Ожидаемый результат

После выполнения всех шагов:
- Веб-приложение должно запуститься без ошибок
- В консоли браузера не должно быть ошибок о несовместимости версий React
- Экран приложения должен отображаться корректно

