# QuickNotes

Кроссплатформенный сервис заметок для Web, Android и iOS с синхронизацией данных через облако.

## 🏗️ Архитектура

Проект организован как монорепозиторий с двумя основными пакетами:

```
quicknotes/
├── packages/
│   ├── backend/          # Node.js + Express + PostgreSQL API
│   └── mobile/           # React Native + Expo приложение
├── package.json          # Корневой package.json для управления монорепозиторием
└── README.md
```

### Backend (`packages/backend`)
- **Технологии**: Node.js, TypeScript, Express, PostgreSQL (Neon), Prisma
- **Функции**: REST API для аутентификации и управления заметками
- **Аутентификация**: JWT токены

### Mobile/Web App (`packages/mobile`)
- **Технологии**: React Native, Expo, React Native Web, TypeScript
- **Платформы**: iOS, Android, Web
- **Функции**: UI для работы с заметками, синхронизация с backend

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- PostgreSQL база данных (рекомендуется Neon)
- Для мобильной разработки: Expo CLI и Expo Go на устройстве (опционально)

### Установка

```powershell
# Клонирование репозитория (если нужно)
# git clone <repository-url>
# cd quicknotes

# Установка зависимостей для всех пакетов
npm install

# Или установка по отдельности
cd packages/backend
npm install

cd ../mobile
npm install
```

### Настройка Backend

```powershell
cd packages/backend

# Скопируйте .env.example в .env
Copy-Item .env.example .env

# Отредактируйте .env и укажите:
# - DATABASE_URL (строка подключения к PostgreSQL на Neon)
# - JWT_SECRET (секретный ключ для JWT токенов)

# Примените миграции базы данных
npx prisma migrate dev

# Запустите сервер разработки
npm run dev
```

Backend будет доступен на `http://localhost:3000`

### Настройка Mobile App

```powershell
cd packages/mobile

# Создайте .env файл
# EXPO_PUBLIC_API_URL=http://localhost:3000/api

# Запустите приложение
npm start
```

Выберите платформу в интерактивном меню Expo:
- `w` - для веб-браузера
- `a` - для Android эмулятора/устройства
- `i` - для iOS симулятора (требует macOS)

## 📁 Структура проекта

### Backend структура

```
packages/backend/
├── src/
│   ├── controllers/     # Обработчики HTTP запросов
│   ├── services/         # Бизнес-логика
│   ├── middleware/       # Express middleware (auth, validation)
│   ├── routes/           # API маршруты
│   ├── utils/            # Утилиты (JWT, валидация)
│   └── app.ts            # Инициализация Express
├── prisma/
│   └── schema.prisma     # Схема базы данных
└── package.json
```

### Mobile структура

```
packages/mobile/
├── src/
│   ├── screens/          # Экраны приложения
│   ├── components/       # UI компоненты
│   ├── navigation/       # Навигация
│   ├── services/         # API клиенты
│   ├── store/            # Управление состоянием
│   └── App.tsx           # Корневой компонент
└── package.json
```

## 🔑 Основные функции

### Аутентификация
- Регистрация пользователя (email + пароль)
- Вход в систему
- JWT токены для безопасной аутентификации
- Защищенные маршруты API

### Управление заметками
- Создание заметок
- Редактирование заметок
- Удаление заметок
- Архивирование заметок
- Просмотр списка заметок

### Синхронизация
- Автоматическая синхронизация между устройствами
- Офлайн режим с локальным кешем
- Конфликт-резолюция при синхронизации

## 📊 Модель данных

### User (Пользователь)
- `id` - уникальный идентификатор
- `email` - email адрес (уникальный)
- `password` - хеш пароля
- `createdAt` - дата создания
- `updatedAt` - дата обновления

### Note (Заметка)
- `id` - уникальный идентификатор
- `userId` - ID пользователя (внешний ключ)
- `title` - заголовок заметки
- `content` - содержимое заметки
- `isArchived` - флаг архивации
- `createdAt` - дата создания
- `updatedAt` - дата последнего обновления

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Заметки
- `GET /api/notes` - Список заметок
- `GET /api/notes/:id` - Получить заметку
- `POST /api/notes` - Создать заметку
- `PUT /api/notes/:id` - Обновить заметку
- `DELETE /api/notes/:id` - Удалить заметку
- `PATCH /api/notes/:id/archive` - Архивировать/разархивировать

## 🛠️ Разработка

### Запуск в режиме разработки

```powershell
# Terminal 1: Backend
cd packages/backend
npm run dev

# Terminal 2: Mobile App
cd packages/mobile
npm start
```

### Тестирование

```powershell
# Backend тесты
cd packages/backend
npm test

# Mobile тесты
cd packages/mobile
npm test
```

## 📦 Сборка для production

### Backend

```powershell
cd packages/backend
npm run build
npm start
```

### Mobile App

```powershell
cd packages/mobile
npm run build:android   # Android
npm run build:ios       # iOS
npm run build:web       # Web
```

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- Валидация входных данных
- Защита от SQL инъекций через Prisma
- CORS настройки для безопасности

## 📝 Лицензия

См. файл LICENSE

## 🤝 Вклад

1. Создайте ветку для новой функции
2. Внесите изменения
3. Создайте Pull Request

## 📞 Поддержка

Для вопросов и предложений создайте Issue в репозитории.
